import axios from 'axios';

import { type IEntityUuidIdDTO } from '@/shared/model/entity-uuid-id-dto.model';

const baseApiUrl = 'api/entity-uuid-id-dtos';

export default class EntityUuidIdDTOService {
  find(id: string): Promise<IEntityUuidIdDTO> {
    return new Promise<IEntityUuidIdDTO>((resolve, reject) => {
      axios
        .get(`${baseApiUrl}/${id}`)
        .then(res => {
          resolve(res.data);
        })
        .catch(err => {
          reject(err);
        });
    });
  }

  retrieve(): Promise<any> {
    return new Promise<any>((resolve, reject) => {
      axios
        .get(baseApiUrl)
        .then(res => {
          resolve(res);
        })
        .catch(err => {
          reject(err);
        });
    });
  }

  delete(id: string): Promise<any> {
    return new Promise<any>((resolve, reject) => {
      axios
        .delete(`${baseApiUrl}/${id}`)
        .then(res => {
          resolve(res);
        })
        .catch(err => {
          reject(err);
        });
    });
  }

  create(entity: IEntityUuidIdDTO): Promise<IEntityUuidIdDTO> {
    return new Promise<IEntityUuidIdDTO>((resolve, reject) => {
      axios
        .post(`${baseApiUrl}`, entity)
        .then(res => {
          resolve(res.data);
        })
        .catch(err => {
          reject(err);
        });
    });
  }

  update(entity: IEntityUuidIdDTO): Promise<IEntityUuidIdDTO> {
    return new Promise<IEntityUuidIdDTO>((resolve, reject) => {
      axios
        .put(`${baseApiUrl}/${entity.id}`, entity)
        .then(res => {
          resolve(res.data);
        })
        .catch(err => {
          reject(err);
        });
    });
  }

  partialUpdate(entity: IEntityUuidIdDTO): Promise<IEntityUuidIdDTO> {
    return new Promise<IEntityUuidIdDTO>((resolve, reject) => {
      axios
        .patch(`${baseApiUrl}/${entity.id}`, entity)
        .then(res => {
          resolve(res.data);
        })
        .catch(err => {
          reject(err);
        });
    });
  }
}
